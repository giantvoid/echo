{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for PureType.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
